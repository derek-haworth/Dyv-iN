
	{{!-- If login error, display here --}}
	<div class="row">
		{{#if loginErrors}}
			<div class="col-md-12">
	    		<div class="alert alert-danger text-center">{{loginErrors}}</div>
	    	<div>
		{{/if}}
	</div>
	
	{{!-- City Search --}}
	<div class="row">
		<div class="col-md-12">
			<div id="welcomeInquiryBox">
				<h2 id="welcomeInquiry">Where are we looking into today...</h2>
				<form id="citySearchForm" method="GET" action="/categories">
					<div class="form-group">
			 			<input id="citySearch" type="text" name="citySearch" value="Chicago">
			  			<div id="registrationSubmit"><input type="submit" value="Submit"></div>
			  		</div>
				</form> 
			</div>
		</div>
	</div>


