<div class="row">
    <div class="col-lg-12">
    	<div class="card">
  			<div class="card-header">
    			{{posts.type}}
  			</div>
  			<div class="card-body">
    			<h5 class="card-title">{{posts.name}}</h5>
    			<p class="card-text">{{posts.review}}</p>
    			<a href="#" class="btn btn-primary">{{posts.name}}</a>
  			</div>
		</div>
	</div>
   </div>