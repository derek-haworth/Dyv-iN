<h1>This is the main landing page after login</h1>
{{!-- {{#each category}}
	<div class="row">
		<div class="col-md-12">
			<h2>{{this.category_name}}</h2>
		</div>
	</div>
	<div class="row">
		{{#if this.places}}
			{{#each this.places}}
				<div class="col-md-3 text-center">
					<span>Place Name:</span><p>{{this.name}}</p>
					<span>Place Address:</span><p>{{this.address}}</p>
					<span>Place Review:</span><p>{{this.review}}</p>
				</div>
			{{/each}}
		{{/if}}
	</div>
{{/each}}

 --}}

 <div class="col-md-12">
    <form id="posts">
      <div class="form-group">
        <label for="title">Title</label>
        <input placeholder="Post Title" type="text" class="form-control" id="title">
        <label for="body">Body</label>
        <textarea placeholder="Body" class="form-control" rows="10" id="postBody"></textarea>
        <div class="form-group">
          <label for="category">Select Place:</label>
          <select class="form-control" id="place"></select>
        </div>
        <button type="submit" class="btn btn-success submit">Submit</button>
      </div>
    </form>
  </div>